#!/usr/bin/env bash

DISPLAY=${DISPLAY:-:0}
dsp=${1:-${DISPLAY:1}}

socat -x tcp-listen:$((6000+dsp)),reuseaddr unix:/tmp/.X11-unix/X${dsp} &
SOCAT_PID=$!
trap "kill $SOCAT_PID" EXIT

/usr/bin/env bashwm $dsp

